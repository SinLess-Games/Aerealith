{
  "$schema": "https://json-schema.org/schema",
  "type": "object",
  "cliName": "service",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the service (for example: auth)",
      "x-prompt": "What is the service name?"
    }
  },
  "required": ["name"]
}
