// The bot follows the monorepo's shared TypeScript and import rules without
// adding integration-specific exceptions.
import baseConfig from '../../../eslint.config.mjs';

export default [...baseConfig];
