apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ztunnel
  namespace: istio-system
  labels:
    app.kubernetes.io/name: ztunnel
    app.kubernetes.io/instance: ztunnel
    app.kubernetes.io/component: ambient-node-proxy
    app.kubernetes.io/part-of: aerealith-platform
    app.kubernetes.io/managed-by: flux

spec:
  interval: 30m

  dependsOn:
    - name: istio-cni
      namespace: istio-system

  releaseName: ztunnel
  targetNamespace: istio-system
  storageNamespace: istio-system

  chart:
    spec:
      chart: ztunnel
      version: '1.30.4'
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: flux-system
      interval: 12h

  valuesFrom:
    - kind: ConfigMap
      name: ztunnel-values
      valuesKey: values.yaml

  install:
    createNamespace: false
    remediation:
      retries: 3

  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
      strategy: rollback

  rollback:
    cleanupOnFail: true
    timeout: 10m

  timeout: 10m
