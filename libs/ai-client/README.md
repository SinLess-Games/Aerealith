# Aerealith AI Client

Typed browser/server client for the Aerealith AI API.

The client centralizes run creation, idempotency, history, status, cancellation,
usage, model discovery, artifacts, and lifecycle event streams so UI code does
not depend directly on raw API route details.
