{
  "name": "db",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/db/src",
  "projectType": "library",
  "tags": ["scope:db", "type:lib"],
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "drizzle-kit generate --config libs/db/drizzle.config.ts"
      }
    },
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "drizzle-kit migrate --config libs/db/drizzle.config.ts"
      }
    },

    "seed-admin": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx libs/db/seeds/users/seed-platform-owner.ts"
      }
    },

    "seed": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx libs/db/seeds/seed.ts"
      }
    },

    "seed-discord": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx libs/db/seeds/discord/seed-discord.ts"
      }
    },

    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p libs/db/tsconfig.lib.json",
          "tsc --noEmit -p libs/db/tsconfig.seeds.json"
        ],
        "parallel": false
      }
    },

    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/db",
        "main": "libs/db/src/index.ts",
        "tsConfig": "libs/db/tsconfig.lib.json",
        "assets": ["libs/db/*.md"]
      }
    }
  }
}
